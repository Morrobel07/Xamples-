@model IEnumerable<int>

    <!DOCTYPE html>
    <html>
        <body>
            <h2>Números Pares hasta el límite</h2>

<ul>
@foreach (var num in Model)
{
    <li>@num</li>
}
</ul>
        </body>
    </html>